{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node server.js",
    "build": "node --check server.js && node --test tests/config.test.js",
    "start": "node server.js",
    "db:push": "node -e \"console.log('No database migration step required')\"",
    "test": "node --test tests/config.test.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cookie-parser": "^1.4.7",
    "dockerode": "^4.0.6",
    "express": "^4.21.2",
    "yaml": "^2.8.0"
  }
}
